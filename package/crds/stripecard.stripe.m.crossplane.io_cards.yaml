---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cards.stripecard.stripe.m.crossplane.io
spec:
  group: stripecard.stripe.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - stripe
    kind: Card
    listKind: CardList
    plural: cards
    singular: card
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Card is the Schema for the Cards API. The Stripe Card can be
          created, modified, configured and removed by this resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CardSpec defines the desired state of Card
            properties:
              forProvider:
                properties:
                  address:
                    additionalProperties:
                      type: string
                    description: |-
                      Map(String). Address map with fields related to the address: line1, line2, city, state
                      , zip and country.
                      Address map with fields related to the address: line1, line2, city, state, zip and country
                    type: object
                    x-kubernetes-map-type: granular
                  customer:
                    description: |-
                      String. The customer that this card belongs to.
                      The customer that this card belongs to.
                    type: string
                  cvcSecretRef:
                    description: |-
                      Int. Card security code. Highly recommended to always include this value, but it's required only
                      for accounts based in European countries.
                      Card security code. Highly recommended to always include this value, but it's required only for accounts based in European countries.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  expMonth:
                    description: |-
                      Int. Number representing the card's expiration month.
                      Two-digit number representing the card's expiration month.
                    type: number
                  expYear:
                    description: |-
                      Int. Four-digit number representing the card's expiration year.
                      Four-digit number representing the card's expiration year.
                    type: number
                  metadata:
                    additionalProperties:
                      type: string
                    description: |-
                      Map(String). Set of key-value pairs that you can attach to an object. This can be useful for
                      storing additional information about the object in a structured format.
                      Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      String. Cardholder name.
                      Cardholder name.
                    type: string
                  numberSecretRef:
                    description: |-
                      String. The card number, as a string without any separators.
                      The card number, as a string without any separators.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  address:
                    additionalProperties:
                      type: string
                    description: |-
                      Map(String). Address map with fields related to the address: line1, line2, city, state
                      , zip and country.
                      Address map with fields related to the address: line1, line2, city, state, zip and country
                    type: object
                    x-kubernetes-map-type: granular
                  customer:
                    description: |-
                      String. The customer that this card belongs to.
                      The customer that this card belongs to.
                    type: string
                  cvcSecretRef:
                    description: |-
                      Int. Card security code. Highly recommended to always include this value, but it's required only
                      for accounts based in European countries.
                      Card security code. Highly recommended to always include this value, but it's required only for accounts based in European countries.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  expMonth:
                    description: |-
                      Int. Number representing the card's expiration month.
                      Two-digit number representing the card's expiration month.
                    type: number
                  expYear:
                    description: |-
                      Int. Four-digit number representing the card's expiration year.
                      Four-digit number representing the card's expiration year.
                    type: number
                  metadata:
                    additionalProperties:
                      type: string
                    description: |-
                      Map(String). Set of key-value pairs that you can attach to an object. This can be useful for
                      storing additional information about the object in a structured format.
                      Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      String. Cardholder name.
                      Cardholder name.
                    type: string
                  numberSecretRef:
                    description: |-
                      String. The card number, as a string without any separators.
                      The card number, as a string without any separators.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - numberSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.customer is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.customer)
                || (has(self.initProvider) && has(self.initProvider.customer))'
            - message: spec.forProvider.expMonth is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.expMonth)
                || (has(self.initProvider) && has(self.initProvider.expMonth))'
            - message: spec.forProvider.expYear is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.expYear)
                || (has(self.initProvider) && has(self.initProvider.expYear))'
            - message: spec.forProvider.numberSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.numberSecretRef)'
          status:
            description: CardStatus defines the observed state of Card.
            properties:
              atProvider:
                properties:
                  address:
                    additionalProperties:
                      type: string
                    description: |-
                      Map(String). Address map with fields related to the address: line1, line2, city, state
                      , zip and country.
                      Address map with fields related to the address: line1, line2, city, state, zip and country
                    type: object
                    x-kubernetes-map-type: granular
                  addressLine1Check:
                    description: |-
                      String. If address line1 was provided, results of the check: pass, fail, unavailable,
                      or unchecked.
                      If address_line1 was provided, results of the check: pass, fail, unavailable, or unchecked.
                    type: string
                  addressZipCheck:
                    description: |-
                      String. If address zip was provided, results of the check: pass, fail, unavailable,
                      or unchecked.
                      If address_zip was provided, results of the check: pass, fail, unavailable, or unchecked.
                    type: string
                  availablePayoutMethods:
                    description: |-
                      List(String). A set of available payout methods for this card. Only values from this set
                      should be passed as the method when creating a payout.
                      A set of available payout methods for this card. Only values from this set should be passed as the method when creating a payout.
                    items:
                      type: string
                    type: array
                  brand:
                    description: |-
                      String. Card brand. Can be American Express, Diners Club, Discover, JCB, MasterCard, UnionPay
                      , Visa, or Unknown.
                      Card brand. Can be American Express, Diners Club, Discover, JCB, MasterCard, UnionPay, Visa, or Unknown.
                    type: string
                  country:
                    description: |-
                      String. Two-letter ISO code representing the country of the card. You could use this attribute to get a
                      sense of the international breakdown of cards you’ve collected.
                      Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you’ve collected.
                    type: string
                  customer:
                    description: |-
                      String. The customer that this card belongs to.
                      The customer that this card belongs to.
                    type: string
                  cvcCheck:
                    description: |-
                      String. If a cvc was provided, results of the check: pass, fail, unavailable, or unchecked. A
                      result of unchecked indicates that CVC was provided but hasn’t been checked yet
                      If a CVC was provided, results of the check: pass, fail, unavailable, or unchecked. A result of unchecked indicates that CVC was provided but hasn’t been checked yet.
                    type: string
                  expMonth:
                    description: |-
                      Int. Number representing the card's expiration month.
                      Two-digit number representing the card's expiration month.
                    type: number
                  expYear:
                    description: |-
                      Int. Four-digit number representing the card's expiration year.
                      Four-digit number representing the card's expiration year.
                    type: number
                  fingerprint:
                    description: |-
                      String. Uniquely identifies this particular card number. You can use this attribute to check whether
                      two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize
                      card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card
                      number.
                      Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
                    type: string
                  funding:
                    description: |-
                      String. Card funding type. Can be credit, debit, prepaid, or unknown.
                      Card funding type. Can be credit, debit, prepaid, or unknown.
                    type: string
                  id:
                    description: String. The unique identifier for the object.
                    type: string
                  last4:
                    description: |-
                      String. The last four digits of the card.
                      The last four digits of the card.
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: |-
                      Map(String). Set of key-value pairs that you can attach to an object. This can be useful for
                      storing additional information about the object in a structured format.
                      Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      String. Cardholder name.
                      Cardholder name.
                    type: string
                  tokenizationMethod:
                    description: |-
                      String. If the card number is tokenized, this is the method that was used. Can
                      be android_pay (includes Google Pay), apple_pay, masterpass, visa_checkout, or null.
                      If the card number is tokenized, this is the method that was used. Can be android_pay (includes Google Pay), apple_pay, masterpass, visa_checkout, or null.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
