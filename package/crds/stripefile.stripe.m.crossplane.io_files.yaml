---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: files.stripefile.stripe.m.crossplane.io
spec:
  group: stripefile.stripe.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - stripe
    kind: File
    listKind: FileList
    plural: files
    singular: file
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: File is the Schema for the Files API. The Stripe File can be
          created by this resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FileSpec defines the desired state of File
            properties:
              forProvider:
                properties:
                  base64content:
                    description: |-
                      String.
                      A content file to upload encoded by base64.
                    type: string
                  filename:
                    description: |-
                      String. The suitable name for saving the file to a filesystem.
                      The suitable name for saving the file to a filesystem.
                    type: string
                  linkData:
                    description: Optional parameters that automatically create a file
                      link for the newly created file.
                    items:
                      properties:
                        create:
                          description: |-
                            Bool. Set this to true to create a file link for the newly created file.
                            Creating a link is only possible when the file’s purpose is one of the following: business_icon, business_logo,
                            customer_signature, dispute_evidence, pci_document, tax_document_user_upload, or terminal_reader_splashscreen.
                            Set this to true to create a file link for the newly created file. Creating a link is only possible when the file’s purpose is one of the following: business_icon, business_logo, customer_signature, dispute_evidence, pci_document, tax_document_user_upload, or terminal_reader_splashscreen.
                          type: boolean
                        expiresAt:
                          description: |-
                            Int. The link isn’t available after this future timestamp.
                            The link isn’t available after this future timestamp.
                          type: number
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            Map(String). Set of key-value pairs that you can attach to an object.
                            This can be useful for storing additional information about the object in a structured format.
                            Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
                          type: object
                          x-kubernetes-map-type: granular
                      type: object
                    type: array
                  purpose:
                    description: |-
                      String. The purpose of the uploaded file. One of these values are accepted: account_requirement,
                      additional_verification, business_icon, business_logo, customer_signature, dispute_evidence,
                      document_provider_identity_document, finance_report_run, identity_document, identity_document_downloadable,
                      pci_document, selfie, sigma_scheduled_query, tax_document_user_upload, terminal_reader_splashscreen
                      The purpose of the uploaded file.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  base64content:
                    description: |-
                      String.
                      A content file to upload encoded by base64.
                    type: string
                  filename:
                    description: |-
                      String. The suitable name for saving the file to a filesystem.
                      The suitable name for saving the file to a filesystem.
                    type: string
                  linkData:
                    description: Optional parameters that automatically create a file
                      link for the newly created file.
                    items:
                      properties:
                        create:
                          description: |-
                            Bool. Set this to true to create a file link for the newly created file.
                            Creating a link is only possible when the file’s purpose is one of the following: business_icon, business_logo,
                            customer_signature, dispute_evidence, pci_document, tax_document_user_upload, or terminal_reader_splashscreen.
                            Set this to true to create a file link for the newly created file. Creating a link is only possible when the file’s purpose is one of the following: business_icon, business_logo, customer_signature, dispute_evidence, pci_document, tax_document_user_upload, or terminal_reader_splashscreen.
                          type: boolean
                        expiresAt:
                          description: |-
                            Int. The link isn’t available after this future timestamp.
                            The link isn’t available after this future timestamp.
                          type: number
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            Map(String). Set of key-value pairs that you can attach to an object.
                            This can be useful for storing additional information about the object in a structured format.
                            Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
                          type: object
                          x-kubernetes-map-type: granular
                      type: object
                    type: array
                  purpose:
                    description: |-
                      String. The purpose of the uploaded file. One of these values are accepted: account_requirement,
                      additional_verification, business_icon, business_logo, customer_signature, dispute_evidence,
                      document_provider_identity_document, finance_report_run, identity_document, identity_document_downloadable,
                      pci_document, selfie, sigma_scheduled_query, tax_document_user_upload, terminal_reader_splashscreen
                      The purpose of the uploaded file.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.base64content is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.base64content)
                || (has(self.initProvider) && has(self.initProvider.base64content))'
            - message: spec.forProvider.filename is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.filename)
                || (has(self.initProvider) && has(self.initProvider.filename))'
            - message: spec.forProvider.purpose is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.purpose)
                || (has(self.initProvider) && has(self.initProvider.purpose))'
          status:
            description: FileStatus defines the observed state of File.
            properties:
              atProvider:
                properties:
                  base64content:
                    description: |-
                      String.
                      A content file to upload encoded by base64.
                    type: string
                  created:
                    description: |-
                      Int. Time at which the object was created. Measured in seconds since the Unix epoch.
                      Time at which the object was created. Measured in seconds since the Unix epoch.
                    type: number
                  expiresAt:
                    description: |-
                      Int. The link isn’t available after this future timestamp.
                      The file expires and isn’t available at this time in epoch seconds.
                    type: number
                  filename:
                    description: |-
                      String. The suitable name for saving the file to a filesystem.
                      The suitable name for saving the file to a filesystem.
                    type: string
                  id:
                    description: String. The unique identifier for the object.
                    type: string
                  linkData:
                    description: Optional parameters that automatically create a file
                      link for the newly created file.
                    items:
                      properties:
                        create:
                          description: |-
                            Bool. Set this to true to create a file link for the newly created file.
                            Creating a link is only possible when the file’s purpose is one of the following: business_icon, business_logo,
                            customer_signature, dispute_evidence, pci_document, tax_document_user_upload, or terminal_reader_splashscreen.
                            Set this to true to create a file link for the newly created file. Creating a link is only possible when the file’s purpose is one of the following: business_icon, business_logo, customer_signature, dispute_evidence, pci_document, tax_document_user_upload, or terminal_reader_splashscreen.
                          type: boolean
                        expiresAt:
                          description: |-
                            Int. The link isn’t available after this future timestamp.
                            The link isn’t available after this future timestamp.
                          type: number
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            Map(String). Set of key-value pairs that you can attach to an object.
                            This can be useful for storing additional information about the object in a structured format.
                            Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
                          type: object
                          x-kubernetes-map-type: granular
                      type: object
                    type: array
                  links:
                    description: |-
                      List(Resource). A list of file links that point at this file.
                      Please see details of links.
                    items:
                      properties:
                        created:
                          description: Int. Time at which the object was created.
                            Measured in seconds since the Unix epoch.
                          type: number
                        expired:
                          description: Bool. Returns if the link is already expired.
                          type: boolean
                        expiresAt:
                          description: Int. The link isn’t available after this future
                            timestamp.
                          type: number
                        id:
                          description: String. The unique identifier for the object.
                          type: string
                        livemode:
                          description: |-
                            Bool. Has the value true if the object exists in live mode or the value false
                            if the object exists in test mode.
                          type: boolean
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            Map(String). Set of key-value pairs that you can attach to an object.
                            This can be useful for storing additional information about the object in a structured format.
                          type: object
                          x-kubernetes-map-type: granular
                        object:
                          description: String. String representing the object’s type.
                            Objects of the same type share the same value.
                          type: string
                        url:
                          description: String. Use your live secret API key to download
                            the file from this URL.
                          type: string
                      type: object
                    type: array
                  object:
                    description: |-
                      String. String representing the object’s type. Objects of the same type share the same value.
                      String representing the object’s type. Objects of the same type share the same value.
                    type: string
                  purpose:
                    description: |-
                      String. The purpose of the uploaded file. One of these values are accepted: account_requirement,
                      additional_verification, business_icon, business_logo, customer_signature, dispute_evidence,
                      document_provider_identity_document, finance_report_run, identity_document, identity_document_downloadable,
                      pci_document, selfie, sigma_scheduled_query, tax_document_user_upload, terminal_reader_splashscreen
                      The purpose of the uploaded file.
                    type: string
                  size:
                    description: |-
                      Int. The size of the file object in bytes.
                      The size of the file object in bytes.
                    type: number
                  type:
                    description: |-
                      String. The returned file type (for example, csv, pdf, jpg, or png).
                      The returned file type (for example, csv, pdf, jpg, or png).
                    type: string
                  url:
                    description: |-
                      String. Use your live secret API key to download the file from this URL.
                      Use your live secret API key to download the file from this URL.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
